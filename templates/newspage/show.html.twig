{% extends 'base.html.twig' %}

{% block title %}Newspage{% endblock %}

{% block body %}
    <h1>Newspage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ newspage.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ newspage.title }}</td>
            </tr>
            <tr>
                <th>Short_desct</th>
                <td>{{ newspage.shortDesct }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ newspage.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('newspage_index') }}">back to list</a>

    <a href="{{ path('newspage_edit', {'id': newspage.id}) }}">edit</a>

    {{ include('newspage/_delete_form.html.twig') }}
{% endblock %}
